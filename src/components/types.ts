export interface Tag {
  name: string;
  colour?: string;
}

/* eslint-disable no-unused-vars */
export enum Themes {
  theme = "theme",
  light = "light",
  dark = "dark",
}
/* eslint-enable no-unused-vars */
